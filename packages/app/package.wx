import { ciFacet } from 'wx:/stacks/ts-executable'

export default {
  ci: ciFacet({
    name: 'app',
    scope: 'caeus',
    deps: [
      { local: 'common' },
      { remote: '@orpc/server' },
      { remote: 'wyr-ts' },
    ]
  })
}
