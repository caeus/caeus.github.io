import { stack } from 'wx:/stacks/ts-lib'

export default stack({
  name: 'common',
  scope: 'caeus',
  version: '0.1.0',
  deps: [
    { remote: '@orpc/contract' },
    { remote: '@orpc/zod' },
    { remote: 'zod' },
  ]
})
