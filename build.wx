export default {
  ci: {
    deploy: {
      deps: ['packages/ui#ci#build'],
      exports: [{ from: '/docs', to: 'docs' }],
      run: (deps) => [
        { FROM: deps['packages/ui#ci#build'] },
      ]
    }
  }
}
