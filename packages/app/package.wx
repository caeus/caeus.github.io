import { stack } from 'wx:/stacks/ts-executable'

export default stack({
  name: 'app',
  scope: 'caeus',
  version: '0.1.0',
  deps: [
    { local: 'common', kind: 'prod' },
    { remote: '@orpc/server', kind: 'prod' },
    { remote: '@caeus/wyr', kind: 'prod' },
  ]
})
