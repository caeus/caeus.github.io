name: Tests

on:
  pull_request:

permissions:
  contents: read

jobs:
  worxpace:
    name: Worxpace
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Build Worxpace
        run: docker build -t worxpace-test ./worxpace

      - name: Run Worxpace tests
        run: docker run --rm --entrypoint pnpm worxpace-test test

  targets:
    name: Test targets
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Discover and run test targets
        shell: bash
        run: |
          ./worxpace/cli.sh list | tee /tmp/worxpace-targets.txt
          mapfile -t targets < <(
            awk '{
              target = $0
              sub(/\[.*/, "", target)
              if (target ~ /^[^#]+#[^#]+#test$/) print target
            }' /tmp/worxpace-targets.txt
          )

          if ((${#targets[@]} == 0)); then
            echo 'No #test targets found' >&2
            exit 1
          fi

          printf 'Running %d test target(s):\n' "${#targets[@]}"
          printf '  %s\n' "${targets[@]}"
          ./worxpace/cli.sh run "${targets[@]}"
