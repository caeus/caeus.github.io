import { ciFacet } from 'wx:/stacks/ts-lib'

export default {
  ci: ciFacet({
    name: 'common',
    scope: 'caeus',
    deps: [
      { remote: '@orpc/contract' },
      { remote: '@orpc/zod' },
      { remote: 'zod' },
    ]
  })
}
